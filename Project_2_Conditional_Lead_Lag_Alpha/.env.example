# Copy to `.env` (git-ignored) and fill in.  `.env` is loaded automatically by
# src/lead_lag/data/wrds_connection.py via python-dotenv; never commit the real file.
#
# WRDS (https://wrds-www.wharton.upenn.edu) — used by data/wrds_connection.py
WRDS_USERNAME=
WRDS_PASSWORD=

# Other API keys for the project (add as needed)
# FRED_API_KEY=
