"""
Part 4 — Portfolio Optimization & Risk (not part 1; owned by teammate 4).

Long/short construction, market and sector neutrality, optimization, factor
exposures, Sharpe, VaR / expected shortfall, drawdown.

Part 1 supplies the two inputs that constrain what is buildable:
`universe.Universe` (which names are tradeable in a given month, and the
`med_dollar_vol` that caps position size) and `wrds_fetch.FactorsDaily` (the
factor returns the exposure regressions run against).
"""
